// Common module — shared types and utilities for all compiler phases.
// This stub declares only the arena submodule for now. Other submodules
// (diagnostics, source_map, intern, numeric) will be declared by their
// respective assigned agents.

pub mod arena;
